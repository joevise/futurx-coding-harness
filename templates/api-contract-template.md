# API: {service-name}

> 模板。复制成 `<your-service>.md` 后填写。
> 推荐复杂项目用 OpenAPI 3 yaml；简单项目用本 markdown 模板。

## 元信息
- **服务**：
- **基地址**：
- **版本**：v1
- **owner**：

---

## 端点

### `POST /api/example`

**用途**：

**请求**：
```json
{
  "field": "string"
}
```

**响应 200**：
```json
{
  "id": "string",
  "field": "string"
}
```

**错误**：
- 400 参数错
- 401 未鉴权

**关联**：US-XXX / PRD §X.Y
